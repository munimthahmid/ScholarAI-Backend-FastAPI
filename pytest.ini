[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers -k "not test_all_api_clients"
asyncio_mode = auto
markers =
    unit: Unit tests
    integration: Integration tests
    filter: Search filter tests 
